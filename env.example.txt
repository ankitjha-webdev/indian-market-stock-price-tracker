# Copy this file to .env and fill in your values

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/stock_tracker?schema=public"

# Stock Data Source
# Set to "true" to use live NSE data via stock-nse-india package
# Set to "false" or leave unset to use dummy data (default)
USE_REAL_API=false

# Cron Job Secret (for securing cron endpoints)
CRON_SECRET="your-secret-key-here-change-in-production"

# Next Auth (if needed later)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret"

